#include <stdio.h>

int main() {
    // arithmetic operators
    // + (addition)
    // - (substraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement
    // augmented assignment operator  x(operator)=value

    int x = 5;
    int z = x;
    z++;
    int y = 2;
    int suma = x + y;
    printf("La suma de x con y es: %d\n", suma);
    printf("Z value is: %d\n", z);
    x += 2;
    printf("Value of x var is: %d\n", x);

    return 0;
}
